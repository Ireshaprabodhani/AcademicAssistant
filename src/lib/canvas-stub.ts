// Empty stub for the optional `canvas` dependency used by pdf-parse.
// pdf-parse lists canvas as an optional peer dependency; we stub it out
// so the bundler doesn't throw a "cannot find module 'canvas'" error.
export default {};
